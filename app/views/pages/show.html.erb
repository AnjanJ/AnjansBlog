<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <h1><%= @page.title %></h1> <br />
</p>

<p>
  <strong>Body:</strong>
  <p><%= @page.body %></p><br/>
</p>
<%= link_to 'Edit', edit_page_path(@page) %> |
<%= link_to 'Back', pages_path %>
